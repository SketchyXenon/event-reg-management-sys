# ==============================================================
#  .gitignore — ERMS (Event Registration Management System)
#  Place this file in the ROOT of your project repository.
#
#  Rule of thumb: if it contains a password, API key, or is
#  auto-generated, it should NOT be committed to Git.
# ==============================================================


# ── CREDENTIALS & SECRETS ─────────────────────────────────────
# These files contain passwords, API keys, or DB credentials.
# NEVER commit them — add your own locally.

config.php
db_connect.php
.env
.env.*
*.env


# ── PHP DEPENDENCIES (Composer) ───────────────────────────────
# The vendor/ folder is generated by `composer install`.
# Commit composer.json + composer.lock, NOT vendor/.

vendor/
composer.phar


# ── DATABASE FILES ────────────────────────────────────────────
# SQL dumps may contain real student/user data.
# The schema file (event_registration_db.sql) is safe to commit,
# but data dumps and backups should not be.

*_dump.sql
*_backup.sql
*_data.sql
*.sql.gz
*.sql.zip
login_attempts.sql


# ── ERROR & DEBUG LOGS ────────────────────────────────────────
# Logs can expose stack traces, DB credentials, and user data.

*.log
logs/
log/
error_log
php_errors.log


# ── XAMPP / LOCAL SERVER ──────────────────────────────────────
# Local XAMPP config and cache files.

.htaccess.local
php.ini.local
my.ini


# ── OPERATING SYSTEM FILES ────────────────────────────────────

# macOS
.DS_Store
.AppleDouble
.LSOverride
._*

# Windows
Thumbs.db
Thumbs.db:encryptable
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/

# Linux
*~


# ── IDE & EDITOR FILES ────────────────────────────────────────

# VSCode
.vscode/
*.code-workspace

# PhpStorm / JetBrains
.idea/
*.iml
*.iws
*.ipr

# Sublime Text
*.sublime-project
*.sublime-workspace

# Vim / Emacs
*.swp
*.swo
*~
\#*\#
.emacs.desktop


# ── UPLOADED USER FILES ───────────────────────────────────────
# If your app ever allows file uploads, never commit them.

uploads/
public/uploads/
storage/


# ── CACHE & TEMP FILES ────────────────────────────────────────

cache/
tmp/
temp/
*.tmp
*.cache


# ── SESSION FILES ─────────────────────────────────────────────
# PHP session files stored on disk (if using file-based sessions).

sessions/
sess_*


# ── COMPILED / BUILD OUTPUTS ──────────────────────────────────

node_modules/
dist/
build/
*.min.js.map
*.min.css.map


# ── SECURITY: WHAT IS SAFE TO COMMIT ─────────────────────────
#
#  ✓  All .php files EXCEPT config.php and db_connect.php
#  ✓  assets/css/global.css, admin.css
#  ✓  assets/js/global.js, admin.js
#  ✓  event_registration_db.sql  (schema only — no real data)
#  ✓  composer.json, composer.lock
#  ✓  README.md, this .gitignore
#
#  ✗  config.php          → contains SENDGRID_API_KEY
#  ✗  db_connect.php      → contains DB_USER and DB_PASS
#  ✗  vendor/             → auto-generated, can be huge
#  ✗  *.log               → may expose credentials or PII
#  ✗  any *_dump.sql      → may contain real user data
# ==============================================================